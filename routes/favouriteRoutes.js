const express = require('express');
const router = express.Router();

router.post('/addFavourite', async (req, res) => {});

router.get('/favourites', async (req, res) => {});

router.delete('/deleteFavourite', async (req, res) => {});

module.exports = router;
